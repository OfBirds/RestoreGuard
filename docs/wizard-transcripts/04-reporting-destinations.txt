=== Wizard transcript: Report destinations (the `r` menu entry) ===

GENERATED FILE - DO NOT EDIT. Regenerate after any wizard change:
  bash scripts/update-wizard-transcripts.sh
(enforced by ReportingWizardTranscriptTests; user answers appear as «answer».)

Configures all three destinations (folder, S3, MongoDB). S3 secrets go to
root-only files; the Mongo connection string is entered inline. Every live
probe here is a simulated OK — the real wizard write-tests each destination
(folder write, S3 put+delete, Mongo ping) before accepting it.

--------------------------------- dialogue ----------------------------------

--- Report destinations (each audit delivers its JSON report to ALL of these) ---
Currently: none configured — reports go to the default folder /home/you/.local/share/restoreguard/reports
These go in their own file (reporting.json) so another tool — e.g. HCC —
can read the same file and pull the reports back. Answer the three sections;
what you skip is removed.

Save reports to a folder? [y/N]: «y»
  folder path (Enter = /home/you/.local/share/restoreguard/reports): «/var/lib/restoreguard/reports»
  checking write access ... OK — write access verified
  keep only the newest N reports there (Enter = keep all): «30»

Upload reports to S3-compatible object storage (MinIO, Garage, R2, AWS)? [y/N]: «y»
  endpoint URL (e.g. http://192.168.1.10:9000; Enter = cancel): «http://192.168.1.10:9000»
  bucket (must already exist) [restoreguard]: «backups»
  key prefix inside the bucket [restoreguard/]: «rg-reports/»
  region (S3-compatible servers accept the default) [us-east-1]: «Enter»
  is this AWS S3 itself (virtual-host addressing)? [y/N]: «n»
  file holding the access key (recommended; Enter = type it inline): «/etc/restoreguard/s3.access»
  file holding the secret key (recommended; Enter = type it inline): «/etc/restoreguard/s3.secret»
  checking (writing + removing a test object) ... OK — write access verified

Insert reports into MongoDB? [y/N]: «y»
  file holding the connection string (mongodb://user:pass@host:27017) (recommended; Enter = type it inline): «Enter»
  connection string (mongodb://user:pass@host:27017) (stored in the config file; Enter = cancel): «mongodb://192.168.1.11:27017»
  database [restoreguard]: «Enter»
  collection [reports]: «Enter»
  checking (ping) ... OK — write access verified

Reports now go to: folder /var/lib/restoreguard/reports, s3 http://192.168.1.10:9000/backups, mongo restoreguard.reports.
Wrote reporting.json (the destinations) and pointed restoreguard.json at it via
"reportingFile": "reporting.json". Hand that same file to HCC to read reports back;
a folder destination always keeps a stable latest.json for easy consumption.
-------------------------------------------------------------------------------

=== destinations live-probed, in order ===
  1. folder /var/lib/restoreguard/reports
  2. s3 http://192.168.1.10:9000/backups
  3. mongo restoreguard.reports

=== resulting restoreguard.json (only POINTS at the destinations) ===
{
  "dockerHosts": [
    {
      "alias": "nas",
      "dockerPath": "docker"
    }
  ],
  "pbsMaxSnapshotAgeHours": 0,
  "reportingFile": "reporting.json"
}

=== resulting reporting.json (the standalone file HCC also reads) ===
{
  "folder": {
    "path": "/var/lib/restoreguard/reports",
    "keepLast": 30
  },
  "s3": {
    "endpoint": "http://192.168.1.10:9000",
    "bucket": "backups",
    "prefix": "rg-reports/",
    "region": "us-east-1",
    "forcePathStyle": true,
    "accessKeyFile": "/etc/restoreguard/s3.access",
    "secretKeyFile": "/etc/restoreguard/s3.secret"
  },
  "mongo": {
    "connectionString": "mongodb://192.168.1.11:27017",
    "database": "restoreguard",
    "collection": "reports"
  }
}
