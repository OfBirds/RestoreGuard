{
  // RestoreGuard report destinations — a STANDALONE file (the main config points at
  // it via "reportingFile": "reporting.json"). It is self-contained on purpose: hand
  // this same file to another tool — e.g. HCC — and it can connect to the exact same
  // destinations and READ the reports RestoreGuard WROTE. Every destination is
  // optional and they all run in parallel. A destination that can't be written fails
  // the audit with exit 1 — delivery problems are as loud as findings. The wizard
  // (`r` in the menu) writes this file for you, live-probing every answer.
  // Comments and trailing commas are allowed.

  // Each destination has: "id" (any string, unique; the wizard auto-generates a GUID,
  // edit it to whatever you like), and "enabled" (default true — RG writes to all
  // enabled destinations). RG stamps each report with its target id + a content hash,
  // so a reader (HCC) can tell where/when a report was saved and dedup across targets.

  // A folder: rg-report-<utc-ts>.json per audit + an atomically-replaced latest.json.
  // keepLast prunes all but the newest N; relative path resolves against THIS file's dir.
  "folder": { "id": "local-spool", "enabled": true, "path": "/var/lib/restoreguard/reports", "keepLast": 30 },

  // Any S3-compatible store (MinIO, Garage, R2, AWS) — the bucket must already exist.
  // forcePathStyle false = AWS virtual-host addressing. Secrets inline or via *File
  // (relative to THIS file's dir, so the file stays portable to HCC).
  "s3": {
    "id": "offsite-minio",
    "enabled": true,
    "endpoint": "http://192.168.1.10:9000",
    "bucket": "backups",
    "prefix": "restoreguard/",
    "region": "us-east-1",
    "forcePathStyle": true,
    "accessKeyFile": "/etc/restoreguard/s3.access",
    "secretKeyFile": "/etc/restoreguard/s3.secret"
  },

  // MongoDB: one document per report. MongoDB-wire-compatible servers (FerretDB) work here.
  "mongo": {
    "id": "mongo-main",
    "enabled": true,
    "connectionStringFile": "/etc/restoreguard/mongo.uri",
    "database": "restoreguard",
    "collection": "reports"
  }
}
